- provide(:title, t("ui.all_courses"))
h1 = t("ui.all_courses")

- if ["admin", "editor"].include?( current_user.try(:role) ) 
  = link_to  t('ui.new_course'), new_course_path, class: "btn btn-default btn-primary"
<br/>  
<br/>  

= will_paginate

/Just using users listing CSS style
ul.users   
  = render @courses
  

= will_paginate
