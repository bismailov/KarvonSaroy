- provide(:title, t("ui.all_lessons"))
h1 #{t("ui.all_lessons")} - #{@course.title.capitalize}

= link_to  t('ui.new_lesson'), new_course_lesson_path, class: "btn btn-large btn-primary"
<br/>  
<br/>  

= will_paginate

/Just using users listing CSS style
ul.users   
  = render @lessons
  

= will_paginate
